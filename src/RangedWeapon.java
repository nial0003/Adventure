public class RangedWeapon extends Weapon {

    public RangedWeapon(String rangedWeaponName, String rangedWeaponDescription, int damage) {
        super(rangedWeaponName, rangedWeaponDescription, damage, true);
    }
}
